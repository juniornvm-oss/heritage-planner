// Copie este arquivo para config.js e preencha com as credenciais do seu projeto.
// A SUPABASE_KEY é a chave publishable/anon (pública por design) — o acesso é
// controlado pelas RLS policies do schema "planner".
window.HP_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_KEY: ""
};
